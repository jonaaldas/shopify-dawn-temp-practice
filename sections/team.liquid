<div class="page-width page-width--narrow section-{{ section.id }}-padding">
  {% if section.settings.title != blank %}
    <div>
      <h2 class="main-page-title page-title h0 center">{{ section.settings.title | escape }}</h2>
      <h2>Hey</h2>
    </div>
  {% endif %}
</div>

{% schema %}
  {
    "name": "team",
    "tag": "div",
    "class": "team-members",
    "settings": [
      {
        "id": "title",
        "type": "text",
        "label": "section-title",
        "default": "Meet our Team"
      }
    ]
  }
{% endschema %}